name: Build

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - ready_for_review

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: self-hosted
    if: ${{ !github.event.pull_request.draft }}
    timeout-minutes: 30
    steps:
      - name: Checkout the pull request branch
        uses: actions/checkout@v4

      - name: Setup xcBeautify
        run: brew install xcbeautify

      - name: Restore Swift packages cache
        uses: actions/cache/restore@v4
        id: packages-cache
        with:
          path: .build
          key: ${{ runner.os }}-${{ runner.name }}-spm-${{ hashFiles('Package.resolved') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.name }}-spm-

      - name: Build for iOS
        run: |
          xcodebuild build \
            -scheme BlueprintModels \
            -destination 'generic/platform=iOS' \
            -skipMacroValidation | xcbeautify

      - name: Save Swift packages cache
        uses: actions/cache/save@v4
        if: always()
        with:
          path: .build
          key: ${{ runner.os }}-${{ runner.name }}-spm-${{ hashFiles('Package.resolved') }}
